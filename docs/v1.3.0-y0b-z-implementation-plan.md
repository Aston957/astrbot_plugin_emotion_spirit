# v1.3.0 Y-0b — Big Five←13维派生 实现 (Z, implementation PR)

> **执行模式**: auto (小模型自主, 不提问).
> **前置**: Y-0a 完成 — `docs/v1.3.0-y0-derivation-backing.md` 存在 (含最终权重).
> **HEAD 起点**: Y-0a commit 之后.
> **不 push, 不 ship, 不 bump.**

## 背景

Y-0 方向: **Big Five 从 13 维派生** (加权平均), 不是 1:1 单维映射. 13 个只读 Big Five 文件保留 Big Five 引用, 由 `to_big_five(13dim)` 派生注入 dict; 1 个写位点 (`personality_feedback` drift 表) 迁 13 维 (drift 落权威模型).

本 PR (Y-0b) = 实现派生 + 注入 + drift 表迁移 + §1.8 规约 + guard test.

## 任务

### 1. 读 PR-A 输出
读 `docs/v1.3.0-y0-derivation-backing.md` 拿**最终权重** (PR-A 可能调整了 Y-0a plan 的提案). 若 PR-A 全保留提案, 用提案值; 若有调整, 用调整后的.

### 2. 实现 `to_big_five(personality)` — `emotion_spirit/utils/persona_profiles.py`

```python
def to_big_five(personality: dict[str, float]) -> dict[str, float]:
    """13 维 Sylanne → Big Five 派生 (加权平均, handbook §1.8).

    Big Five 是 13 维的粗粒度投影. 每个 Big Five = 其 NEO-PI-R facet 对应的
    13 维 dim 加权平均. 详 docs/v1.3.0-y0-derivation-backing.md (文献背书).

    Args:
        personality: 13 维 flat dict (warmth_bias/patience/...). 缺维度用 0.5 兜底.

    Returns:
        {extraversion, neuroticism, agreeableness, conscientiousness, openness},
        每值 clamp 到 [0, 1].
    """
    w = personality.get("warmth_bias", 0.5)
    pat = personality.get("patience", 0.5)
    bp = personality.get("boundary_permeability", 0.5)
    rg = personality.get("relational_gravity", 0.5)
    ip = personality.get("intimacy_pull", 0.5)
    ed = personality.get("expression_drive", 0.5)
    gt = personality.get("gossip_tendency", 0.5)
    ic = personality.get("inner_coherence", 0.5)
    cu = personality.get("curiosity", 0.5)
    pa = personality.get("perception_acuity", 0.5)
    di = personality.get("directness", 0.5)
    eo = personality.get("exploration_openness", 0.5)

    # ⚠️ 权重从 docs/v1.3.0-y0-derivation-backing.md (Y-0a 验证). 下面是提案默认,
    # 若 Y-0a 调了, 用调整后的值.
    E = 0.25*w + 0.25*ed + 0.20*gt + 0.15*ip + 0.15*rg
    N = 0.40*bp + 0.35*(1-ic) + 0.25*(1-pat)
    A = 0.40*w + 0.30*di + 0.30*rg
    C = 0.65*ic + 0.35*pat
    O = 0.40*cu + 0.35*eo + 0.25*pa
    clamp = lambda x: max(0.0, min(1.0, x))
    return {
        "extraversion": clamp(E),
        "neuroticism": clamp(N),
        "agreeableness": clamp(A),
        "conscientiousness": clamp(C),
        "openness": clamp(O),
    }
```

> **flagged**: 若 Y-0a 调了权重, 替换上面的系数. 在函数 docstring 注明 "权重来自 Y-0a, 最后验证日期".

### 3. 注入 `_get_current_personality_dict` — `main.py`

当前 (b01e596 状态):
```python
    def _get_current_personality_dict(self) -> dict[str, Any]:
        try:
            from emotion_spirit.utils import get_personality_params
            return get_personality_params(self._labels)
        except Exception:
            # fallback 保持 flat shape (历史兼容性), v1.2.6 再全局统一
            return {"openness": 0.5, "extraversion": 0.5, "agreeableness": 0.5,
                    "neuroticism": 0.5, "conscientiousness": 0.5}
```

改为 (派生 Big Five 注入 top-level, 13 维保留在 deep/surface):
```python
    def _get_current_personality_dict(self) -> dict[str, Any]:
        try:
            from emotion_spirit.utils import get_personality_params
            from emotion_spirit.utils.persona_profiles import to_big_five
            result = get_personality_params(self._labels)
            # §1.8: 派生 Big Five 注入 top-level (13 维 → Big Five 加权平均)
            flat = {**result.get("deep", {}), **result.get("surface", {})}
            result.update(to_big_five(flat))
            return result
        except Exception:
            # §1.8: fallback 13 维中性 + 派生 Big Five (中性 0.5 → Big Five 0.5)
            from emotion_spirit.core.persona_labels_db import REQUIRED_DIMS
            from emotion_spirit.utils.persona_profiles import to_big_five
            flat = {dim: 0.5 for dim in REQUIRED_DIMS}
            flat.update(to_big_five(flat))
            return flat
```

### 4. 迁 `personality_feedback` drift 表 — `emotion_spirit/regulation/personality_feedback.py`

drift 表 (line ~15) `"creative": {"openness": +0.003, "extraversion": -0.001}` 等是 Big Five key. drift 要落 13 维权威模型, 按**派生权重反向分配**: 每个 Big Five delta 分配到其派生的 13 维 dims (乘以派生权重).

示例 (用提案权重):
- `{"openness": +0.003}` → `{"curiosity": +0.0012, "exploration_openness": +0.00105, "perception_acuity": +0.00075}` (0.003 × 0.40/0.35/0.25)
- `{"extraversion": -0.001}` → `{"warmth_bias": -0.00025, "expression_drive": -0.00025, "gossip_tendency": -0.0002, "intimacy_pull": -0.00015, "relational_gravity": -0.00015}`
- `{"neuroticism": +X}` → `{"boundary_permeability": +0.40X, "inner_coherence": -0.35X, "patience": -0.25X}` (注意 (1-ic) 反向 → ic 减; (1-pat) 反向 → pat 减)
- `{"agreeableness": +X}` → `{"warmth_bias": +0.40X, "directness": +0.30X, "relational_gravity": +0.30X}`
- `{"conscientiousness": +X}` → `{"inner_coherence": +0.65X, "patience": +0.35X}`

对 drift 表里**每个 activity 的每个 Big Five key** 应用上述分配, 13 维 key 累加 (一个 activity 可能多个 Big Five → 同一 13 维 dim 累加). 保留 4 位小数.

> **flagged**: 若 Y-0a 调了权重, 用调整后的权重分配. 若某 13 维 dim 累加后为 0, 仍保留 (显式 > 隐式). 迁完后 `grep "extraversion\|neuroticism\|agreeableness\|conscientiousness\|\"openness\"" personality_feedback.py` 应无 Big Five key.

### 5. handbook §1.8 — 新增 (插在 §1.7 之后, §2 之前)

```markdown
### 1.8 Big Five 必须从 13 维派生, 不得硬编码 (v1.3.0 Y-0)

**规则**：本系统人格 dict 由 `labels_to_personality()` 产出 **13 维 Sylanne**, 不直接产出 Big Five. 引用 Big Five 的轴心功能必须通过 `to_big_five(personality_13dim)` 派生 (加权平均), 不得硬编码 `.get(..., 0.5)` (死耦合) 也不得 1:1 单维替换 (丢多 facet 信息).

**派生公式**：见 `docs/v1.3.0-y0-derivation-backing.md` (NEO-PI-R facet 文献背书). 要点: 每个 Big Five = 其 facet 对应的多个 13 维 dim 加权平均. `warmth_bias` 同时进 E+A (心理学事实, warmth 是 E1 但含 A6 语义).

**写位点 (drift)**：`personality_feedback` 的 drift 表必须用 **13 维 key** (drift 落权威模型), 不得 Big Five. Big Five delta 按派生权重反向分配到 13 维.

**违反会被什么拦下**：`tests/test_big_five_derivation.py`:
- `to_big_five` 在不同 persona 上产出不同 Big Five 值 (派生生效, 非全 0.5)
- `personality_feedback.py` drift 表无 Big Five key

**真实落法**：`_get_current_personality_dict` 调 `to_big_five(flat_13dim)` 注入 top-level; 13 个只读 Big Five 文件不变 (`.get("extraversion")` 拿派生值). `personality_feedback` drift 表迁 13 维.

**引用**：Costa & McCrae 1992 (NEO PI-R Manual); McCrae & John 1992 (本地 KB W2071559616); John & Srivastava 1999 (本地 KB W1641003075). 详 `docs/v1.3.0-y0-derivation-backing.md`.

---
```

### 6. guard test — 新文件 `tests/test_big_five_derivation.py`

```python
"""§1.8: Big Five 必须从 13 维派生. 验证 to_big_five 派生生效 + personality_feedback drift 表无 Big Five."""
import re
from pathlib import Path

REPO_ROOT = Path(__file__).parent.parent
BIG_FIVE = re.compile(r'"(extraversion|neuroticism|agreeableness|conscientiousness|openness)"')


def test_to_big_five_varies_across_personas():
    """to_big_five 在不同 13 维输入上产出不同 Big Five (派生生效, 非全 0.5)."""
    from emotion_spirit.utils.persona_profiles import to_big_five
    # 高 E 低 N persona vs 低 E 高 N persona
    p1 = {"warmth_bias": 0.8, "expression_drive": 0.8, "gossip_tendency": 0.7,
          "intimacy_pull": 0.7, "relational_gravity": 0.6,
          "boundary_permeability": 0.3, "inner_coherence": 0.8, "patience": 0.7,
          "directness": 0.5, "curiosity": 0.5, "perception_acuity": 0.5,
          "exploration_openness": 0.5}
    p2 = {"warmth_bias": 0.2, "expression_drive": 0.2, "gossip_tendency": 0.3,
          "intimacy_pull": 0.3, "relational_gravity": 0.4,
          "boundary_permeability": 0.8, "inner_coherence": 0.3, "patience": 0.3,
          "directness": 0.5, "curiosity": 0.5, "perception_acuity": 0.5,
          "exploration_openness": 0.5}
    b1 = to_big_five(p1)
    b2 = to_big_five(p2)
    assert b1["extraversion"] > b2["extraversion"], "高 E persona 应派生更高 extraversion"
    assert b1["neuroticism"] < b2["neuroticism"], "低 N persona 应派生更低 neuroticism"
    for v in list(b1.values()) + list(b2.values()):
        assert 0.0 <= v <= 1.0, f"Big Five 值越界 [0,1]: {v}"
    # 非全 0.5 (派生真在算)
    assert any(abs(v - 0.5) > 0.01 for v in b1.values()), "b1 不应全 0.5"


def test_personality_feedback_drift_table_no_big_five():
    """§1.8: personality_feedback drift 表必须 13 维 (无 Big Five key)."""
    pf = (REPO_ROOT / "emotion_spirit" / "regulation" / "personality_feedback.py").read_text(encoding="utf-8")
    violations = BIG_FIVE.findall(pf)
    assert not violations, f"personality_feedback 仍有 Big Five key (drift 表没迁): {violations}"
```

### 7. 验证

```bash
cd /d/新建文件夹/emotion_spirit/now/astrbot_plugin_emotion_spirit

# 1. 派生测试 + drift 表无 Big Five
PYTHONPATH=. python -m pytest tests/test_big_five_derivation.py -v

# 2. 全量回归 (注入 + drift 迁移不破其他; 13 个只读文件现在拿派生 Big Five, 应仍 PASS)
PYTHONPATH=. python -m pytest -q 2>&1 | tail -5

# 3. 确认 personality_feedback 无 Big Five
grep -n '"extraversion"\|"neuroticism"\|"agreeableness"\|"conscientiousness"\|"openness"' emotion_spirit/regulation/personality_feedback.py
#   期望: 无输出

# 4. 确认 _get_current_personality_dict 注入了派生
grep -n "to_big_five" main.py
#   期望: happy path + fallback 两处
```

> **flagged**: 若 step 2 全量回归有 failure (某测试断言 personality dict 不含 Big Five, 或断言 fallback 返 Big Five 0.5), 调整测试期望以匹配 §1.8 派生语义. 记录在报告.

### 8. commit (不 push)

```bash
git add emotion_spirit/utils/persona_profiles.py main.py \
        emotion_spirit/regulation/personality_feedback.py \
        UPDATE_HANDBOOK.md tests/test_big_five_derivation.py

git commit -m "feat(v1.3.0-y0b): Big Five←13维派生 (Z) + drift 表迁 13 维 + §1.8 + guard

- to_big_five(13dim): 加权平均派生 5 个 Big Five (NEO-PI-R facet 背书, Y-0a 验证)
- main._get_current_personality_dict: 注入派生 Big Five 到 top-level (13 个只读文件拿真值)
- personality_feedback: drift 表 Big Five→13 维 按派生权重反向分配 (drift 落权威模型)
- handbook §1.8: Big Five 必须派生, 不得硬编码/.get(0.5); drift 必须 13 维
- tests/test_big_five_derivation.py: 派生生效 + drift 表无 Big Five

13 只读文件不迁 (保留 Big Five 引用, 派生注入修活). 1 写位点 (drift 表) 迁 13 维.
NOT pushed/shipped. v1.3.0 开发线."
```

## 不做

- ❌ 迁 13 个只读 Big Five 文件 (保留引用, 派生注入修活)
- ❌ Bug-G 调参 (Y 完后顺手)
- ❌ push / ship / bump / CHANGELOG / README (v1.3.0 ship 时按 §4.6 补)

## 完成后报告

- 新 HEAD SHA
- to_big_five 权重 (是否与 Y-0a 提案一致, 或 Y-0a 调了)
- personality_feedback drift 表迁移后行数 + 13 维 key 数
- 全量测试数 (应 1430 passed 左右, +2 新派生测试)
- `git log --oneline -3`
- 任何偏离 + 原因
- 确认 NOT pushed
