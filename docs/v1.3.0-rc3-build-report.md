# v1.3.0 rc.3 构建报告(2026-07-04, 不 push)

## 范围
Bug-E/H (framework 防护) + Bug-F (memory_type 彻底修). **不 push, 不 bump.**
等用户实测 + push framework issue 后 ship v1.3.0.

HEAD: `b809893` (rc.2 base) → 本次 commit

## 改动清单

### Bug-E/H (framework, emotion_spirit 侧防护)
- _conf_schema.json: delivery_mode 默认 append → event_send (Bug-H 让 append 不可用)
- orchestrator.py: append 模式加诊断 log (chain 内容 + id, INFO)
- docs/framework-issue-bug-h.md: issue 模板 (Bug-H + send_delayed API, 待提 AstrBot)

### Bug-F (memory_type 彻底修)
- unified_entry.py: UnifiedEntry 加 memory_type 字段 (默认 "bot_reply") + to_dict 序列化
- memory_pool.py: add() 加 memory_type 参数 → 传 UnifiedEntry; add_for_user 加 memory_type 参数
- memory_pool.py: search_by_vector 加 exclude_memory_types 过滤参数
- memory_pool.py: recall_semantic 传 exclude_memory_types={"bot_ephemeral_state"}
- main.py: _apply_bot_reply_effects 改标 memory_type (ephemeral 仍入 pool, 不再 '不入 pool')
- main.py: _EPHEMERAL_BOT_TOKENS 注释更新 (不再用于拦截, 只判定 ephemeral 标类型)
- 旧测试更新: test_bot_ephemeral_filter.py (断言从 assert_not_called → 验证 memory_type 参数)

## 测试
- pytest tests/ 全套: **1417 passed**, 0 failed
- 新增: test_delivery_mode_default (1) + test_memory_type (9) = 10
- 更新: test_bot_ephemeral_filter (4, 改断言)
- 已知: test_periodic_save_dirty_only Win flake (v1.2.6 backlog, 本 run 不触发)

## 不做清单
- ❌ git push / bump / CHANGELOG
- ❌ Bug-E 根治 (等 framework send_delayed API)
- ❌ Bug-H 根治 (framework bug, push 上游)

## 实测后后续
1. 用户丢 zip 实测: delivery_mode=event_send (默认) → bot 回复 + 分段 delay (表情包仍失, 等 framework)
2. push AstrBot issue (docs/framework-issue-bug-h.md)
3. 后续 rc: 等 framework 修 Bug-H + 加 send_delayed API → emotion_spirit 启用 append / delayed_append
4. 全绿后 bump v1.3.0 + ship